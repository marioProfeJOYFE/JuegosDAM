<Page x:Class="JuegosDAM.MVVM.View.BibliotecaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:JuegosDAM.MVVM.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BibliotecaPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel 
            Orientation="Vertical"
            Margin="10"
            Grid.Row="0"
            >
            <TextBlock 
        Text="Biblioteca de Juegos"
        Style="{StaticResource TitleTextStyle}"/>

            <TextBlock
                Margin="0,10,0,0"
                Text="Aquí encontrarás todos tus títulos"
                FontSize="16"
                FontWeight="Medium"/>
        </StackPanel>

        <ScrollViewer Margin="0,20,0,0" Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ListaJuegos}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="8" Cursor="Hand">
                            <Border CornerRadius="18"
                                    Background="Beige"
                                    Height="130"
                                    ClipToBounds="True">
                            </Border>
                            <TextBlock Text="{Binding title}"
                                       Margin="4,8,4,2"
                                       FontWeight="SemiBold"
                                />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
    </Grid>
</Page>
